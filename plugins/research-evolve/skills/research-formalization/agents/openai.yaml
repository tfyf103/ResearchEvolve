interface:
  display_name: "Research Formalization"
  short_description: "Audit semantics and formalize conjectures with Lean"
  default_prompt: "Use $research-formalization to run isolated Lean actors against frozen semantics and explain every deterministic gate result."
